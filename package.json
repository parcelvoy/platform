{
  "name": "parcelvoy",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "@types/bcryptjs": "^2.4.2",
    "lerna": "^6.0.3"
  },
  "scripts": {
    "start": "lerna run start",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "test": "lerna run test"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3"
  }
}
